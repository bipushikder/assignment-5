1️. Difference between var, let, and const ?
ans:
            var:
            1. var Can be re-declared and updated.
            2.- Hoisted to the top of its scope 

         let:
             1.- Can be updated, but not re-declared in the same scope.

                 2.Hoisted but not initialized

        const:
            1.- const is a Block-scoped.
            2. const Must be initialized at declaration.
             3. const Cannot be updated or re-declared

  2️. Spread Operator (...) ?

        ans : 
        
            spread operator Expands elements of an array or properties of an object into another  array/object.

   3️. Difference between map(), filter(), and forEach() ?

            ans:
            map() : use to to modify data.

            filter() : When you want a subset then you use it.

            forEach() : when you want side effect loogin, updating then you use it.


  4️. Arrow Function ?
           ans:
           1. A shorter syntax for writing functions.
           2. Arrow function is a short version of function.


  5️. Template Literal ?

        1. Strings disclosed by backticks (`).

        2. you can use this method ${} and multi-line strings.



